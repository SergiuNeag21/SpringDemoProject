# # # H2 Database Configuration
# spring.datasource.url=jdbc:h2:mem:dbtest
# spring.datasource.username=sa
# spring.datasource.password=sa
# spring.datasource.driver-class-name=org.h2.Driver
# # spring.datasource.driver-class-name=org.h2.DriverABC
# # spring.jpa.defer-datasource-initialization=true


# # spring.sql.init.mode=always


# DataSource configuration
spring.datasource.url=jdbc:tc:postgresql:///testdb

# spring.datasource.url=jdbc:postgresql://localhost:5432/testdb?currentSchema=schema.sql
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=create

# Flyway configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Logging
logging.level.socialnetwork=debug
logging.level.org.flywaydb=debug
logging.level.org.springframework=debug

spring.sql.init.mode=always